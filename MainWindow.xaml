<Window x:Class="myplayer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:tools="clr-namespace:MyPlayer"
        Title="MainWindow" Height="350" Width="525">
    <Window.Resources>
        <DataTemplate x:Key="IdParser" DataType="SongDbItems">
            <TextBlock>
                <TextBlock.Text>
                    <Binding Path="Id" />
                </TextBlock.Text>
            </TextBlock>
        </DataTemplate>
        <DataTemplate x:Key="NameParser" DataType="SongDbItems">
            <TextBlock>
                <TextBlock.Text>
                    <Binding Path="Name" />
                </TextBlock.Text>
            </TextBlock>
        </DataTemplate>
        <DataTemplate x:Key="RootDirParser" DataType="SongDbItems">
            <TextBlock>
                <TextBlock.Text>
                    <Binding Path="RootDir" />
                </TextBlock.Text>
            </TextBlock>
        </DataTemplate>
        <DataTemplate x:Key="ArtistParser" DataType="SongDbItems">
            <TextBlock>
                <TextBlock.Text>
                    <Binding Path="Artist" />
                </TextBlock.Text>
            </TextBlock>
        </DataTemplate>
        <DataTemplate x:Key="AlbumParser" DataType="SongDbItems">
            <TextBlock>
                <TextBlock.Text>
                    <Binding Path="Album" />
                </TextBlock.Text>
            </TextBlock>
        </DataTemplate>
        <DataTemplate x:Key="YearParser" DataType="SongDbItems">
            <TextBlock>
                <TextBlock.Text>
                    <Binding Path="Year" />
                </TextBlock.Text>
            </TextBlock>
        </DataTemplate>
        <DataTemplate x:Key="PathParser" DataType="SongDbItems">
            <TextBlock>
                <TextBlock.Text>
                    <Binding Path="Path" />
                </TextBlock.Text>
            </TextBlock>
        </DataTemplate>
    </Window.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" MinWidth="150" />
            <ColumnDefinition Width="3*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="23" />
            <RowDefinition Height="32" />
            <RowDefinition Height="*" />
            <RowDefinition Height="23" />
        </Grid.RowDefinitions>
        <Menu Height="23" Name="menu1" 
              IsMainMenu="True" 
              Grid.Column="0"
              Grid.Row="0"
              Grid.ColumnSpan="2">
            <MenuItem Header="Файл">
                <MenuItem Header="Добавить папку..." />
                <MenuItem Header="Удалить папку..." />
                <MenuItem Header="Создать плейлист" />
                <MenuItem Header="Выход" />
            </MenuItem>
            <MenuItem Header="Справка">
                <MenuItem Header="О программе" />
            </MenuItem>
        </Menu>
        
        <StatusBar Grid.ColumnSpan="2" 
                   Grid.Row="3" 
                   Grid.Column="0"
                   Height="23" 
                   Name="statusBar1" />
        <ListBox Grid.Row="2" 
                 Grid.Column="0"
                 Name="listBox1" 
                 ItemsSource="{Binding}"
                 DataContext="{Binding cases,RelativeSource={RelativeSource Mode=FindAncestor,AncestorType=Window}}"/>
        <ListView Grid.Column="1" 
                  Grid.Row="2" 
                  Name="listView1" ItemsSource="{Binding}" >
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="ID" Width="0" CellTemplate="{DynamicResource IdParser}" />
                    <GridViewColumn Header="Название" CellTemplate="{DynamicResource NameParser}"/>
                    <GridViewColumn Header="Rootdir" Width="0" CellTemplate="{DynamicResource RootDirParser}" />
                    <GridViewColumn Header="Исполнитель" CellTemplate="{DynamicResource ArtistParser}"/>
                    <GridViewColumn Header="Альбом" CellTemplate="{DynamicResource AlbumParser}"/>
                    <GridViewColumn Header="Год" CellTemplate="{DynamicResource YearParser}"/>
                    <GridViewColumn Header="Path" Width="0" CellTemplate="{DynamicResource PathParser}"/>
                </GridView>
            </ListView.View>
        </ListView>
        <Grid Grid.ColumnSpan="2" 
              Grid.Row="1"
              Grid.Column="0"
              Name="grid1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="32" />
                <ColumnDefinition Width="32" />
                <ColumnDefinition Width="32" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="120" />
            </Grid.ColumnDefinitions>
            <Image Grid.Column="0" Source="images\backwardButton.png" Name="Backward"/>
            <Image Grid.Column="1" Source="images\playButton.png" Name="Play"/>
            <Image Grid.Column="1" Source="images\pauseButton.png" Visibility="Hidden" Name="Pause"/>
            <Image Grid.Column="2" Source="images\forwardButton.png" Name="Forward" />
            <Label Content="" Grid.Column="3" Height="28" HorizontalAlignment="Center"
                   Name="label1" VerticalAlignment="Center" />
            <TextBox Grid.Column="4" Grid.Row="0" Height="23" VerticalAlignment="Center">
                <TextBox.Margin>
                    <Thickness Right="10" />
                </TextBox.Margin>
            </TextBox>
        </Grid>       
    </Grid>
</Window>
